{
  "name": "mint-desktop-pet-motion",
  "version": "0.1.0",
  "description": "A desktop companion with resource-aware action switches and action-specific speech.",
  "main": "main.js",
  "author": "Codex",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "check": "node --check main.js && node --check preload.js && node --check src/characters.js && node --check src/renderer.js && node --check src/settings.js",
    "dist": "electron-builder --win portable"
  },
  "devDependencies": {
    "electron": "^37.2.0",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "fun.pet.mintcompanion",
    "productName": "小小雅鹅",
    "asar": true,
    "files": [
      "main.js",
      "preload.js",
      "src/**/*",
      "assets/icon.png",
      "assets/icon.ico",
      "assets/motion/*.png",
      "assets/voices/*.mp3",
      "package.json"
    ],
    "directories": {
      "output": "release"
    },
    "win": {
      "icon": "assets/icon.ico",
      "target": [
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "MintPet-${version}-portable.${ext}"
    }
  }
}
